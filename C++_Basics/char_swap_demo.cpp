#include<iostream>
using namespace std;
int main()
{
	int j,k,l,n;
    string a = "and zone point";
    string b = "a";
    string c = "p";
    string d = "z";
    int len=a.length();
    for(int i=0;i<=len;i++)
    {
    	if(a[i]==b[0])
    	{
    		a[i]==a[n];
    		a[n]==a[i];
    		j=="b";
		}
		else 
		{
			continue;
		}
	}
	for(int i=0;i<=len;i++)
	{
		if(a[i]==c[0])
		{
			k=a[i];
			k="n";
			
		}
		else
		{
			continue;
		}
	}
	for(int i=0;i<=len;i++)
	{
		if(a[i]==c[0])
		{
			l=a[i];
			l="n";
		}
		else
		{
			continue;
		}
    }
    cout<<a[i];
		

}
